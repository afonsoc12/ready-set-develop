---
dotfiles_repo: https://github.com/afonsoc12/dotfiles
# Git commit, branch or tag to checkout
dotfiles_version: HEAD
dotfiles_work_tree: "{{ lookup('env','XDG_CONFIG_HOME') }}"
dotfiles_repo_dest: "{{ lookup('env','XDG_CONFIG_HOME') }}/dotfiles"
dotfiles_extra_zshrc:
