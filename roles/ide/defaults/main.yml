---
# ==================================================
# Task: iterm
# ==================================================
ide_zsh_path: "{{ ansible_env.ZSH | default(zshenv.ZSH) | expandvars }}"

# ==================================================
# Task: vscode
# ==================================================
ide_vscode_app_dir: "{{ homebrew_cask_appdir | default('/Applications') }}/Visual\ Studio\ Code.app1"
ide_vscode_code_bin: "{{ ide_vscode_app_dir }}/Contents/Resources/app/bin/code"
ide_vscode_code_dest_dir: /usr/local/bin
ide_vscode_extensions: []
